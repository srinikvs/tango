{
  "id": "A6",
  "layer": "unit",
  "title": "Version string matches package.json and shipped UI",
  "gate": "optional",
  "steps": [{ "op": "readVersionSources" }],
  "expect": [
    { "assert": "versionMatchesPackage" },
    { "assert": "htmlHasVersionTag", "file": "index.html" },
    { "assert": "uiUsesVersionLabel", "file": "src/game/Game.tsx" }
  ]
}
